pipeline {
  agent {
    dockerfile {
      filename 'Dockerfile'
    }
    
  }
  stages {
    stage('') {
      steps {
        dir(path: '/usr/src/app')
        input 'Working correctly?'
      }
    }
  }
}